%footer.footer
  .container
    .row
      .span7
        %h3 Форма обратной связи
        %h4 Оставьте нам сообщение и мы свяжемся с вами!
        = simple_form_for Web::QuestionEditType.new, wrapper: :question_form, html: { class: 'question-form' } do |f|
          = f.input :content, placeholder: Question.human_attribute_name(:content), wrapper_html: { class: 'pull-right' }

          = f.input :full_name, placeholder: Question.human_attribute_name(:full_name)
          = f.input :email, placeholder: Question.human_attribute_name(:email)
          = f.input :phone, placeholder: Question.human_attribute_name(:phone)

          = f.submit 'Отправить', class: 'btn btn-large btn-danger'

      .span4.info
        = link_to '', root_url, class: 'brand'
        %p Адрес:
        %p 432063, Ульяновск, ул. Ленина, д. 17/118
        %p Телефон:
        %p (8422) 79-45-95, 79-45-96, 79-45-97, 79-45-98
        %p Режим работы:
        %p Будни: с 8:00 до 18:00 (без перерыва на обед)
        %p Выходные: суббота, воскресенье