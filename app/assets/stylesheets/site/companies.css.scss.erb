.companies {
    min-height: 90px;
    border-bottom: 1px solid #8b1919;
    margin-bottom: 20px;

    .company-link {
        margin: 0 auto;
        height: 77px;
        width: 310px;
        display: block;
    }

    .mdm-group {
        background-image: url(<%= asset_path('companies/mdm-group.png') %>);
    }

    .mdm-complect {
        background-image: url(<%= asset_path('companies/mdm-complect.png') %>);
    }

    .index-ventures {
        background-image: url(<%= asset_path('companies/mdm-group.png') %>);
    }
}